// Define to aws lambda handlers
export const joinHandler = async (event: any, context: any, callback: any) => {
  const { body } = event;

  callback(null, "");
};

export const filterHandler = async (
  event: any,
  context: any,
  callback: any
) => {
  const { body } = event;

  callback(null, "");
};
